[server]
port = 3333
host = "0.0.0.0"

[storage]
root = "/data"

[search]
engine = "sqlite"

[search.vector]
enabled = false
# See vault/.kiwi/config.toml for vector search setup options
[versioning]
strategy = "git"

[backup]
remote = ""
interval = "10m"
branch = "main"
rebase_before_push = true
